package com.example.fittrack.viewmodel;

import androidx.lifecycle.ViewModel;

/**
 * ViewModel class for managing the logged workout functionality.
 * This ViewModel is responsible for handling the data and logic related to logged workouts,
 * ensuring that the UI can observe and interact with workout data without directly modifying it.
 */
public class WorkoutViewModel extends ViewModel {

    /**
     * Default constructor for WorkoutViewModel.
     * Initializes the ViewModel for managing logged workout data.
     */
    public WorkoutViewModel() {
        // Constructor logic (if any) can be added here
    }
}
